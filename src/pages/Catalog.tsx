import { products } from '../data/products'
import { NavLink } from 'react-router-dom'
export default function Catalog(){return (<div className="section" style={{background:'#fff',color:'#111'}}><div className="container"><h2 style={{marginTop:0}}>Каталог продукции</h2><div className="grid grid-3">{products.map(p=> (<div key={p.slug} className="tile"><img src={p.image} alt={p.title}/><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><h3>{p.title}</h3><div style={{color:'#667085'}}>{p.specs.slice(0,2).join(' • ')}</div></div><b>{p.price} ₽/м²</b></div><div style={{display:'flex',gap:10,marginTop:12}}><NavLink to={`/catalog/${p.slug}`} className="btn btn-primary">Подробнее</NavLink><a href="/#consult" className="btn">Консультация</a></div></div>))}</div></div></div>) }