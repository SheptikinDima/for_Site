import { useParams, NavLink } from 'react-router-dom'
import { products } from '../data/products'
import Calculator from '../components/Calculator'
export default function Product(){const { slug }=useParams();const p=products.find(x=>x.slug===slug);if(!p) return <div className="section"><div className="container">Товар не найден</div></div>;return (<div className="section" style={{background:'#fff',color:'#111'}}><div className="container" style={{display:'grid',gridTemplateColumns:'1.1fr .9fr',gap:24}}><div><div style={{aspectRatio:'4/3',overflow:'hidden',borderRadius:16,border:'1px solid #e5e7eb'}}><img src={p.image} alt={p.title} style={{width:'100%',height:'100%',objectFit:'cover'}}/></div></div><div><h1 style={{marginTop:0}}>{p.title}</h1><div style={{color:'#667085',marginBottom:10}}>{p.category} • от {p.price} ₽</div><p>{p.description}</p><ul>{p.specs.map(x=> <li key={x}>• {x}</li>)}</ul><div style={{display:'flex',gap:10,marginTop:12}}><a href="/#consult" className="btn btn-primary">Заказать консультацию</a><NavLink to="/projects" className="btn">Смотреть кейсы</NavLink></div><div style={{marginTop:16}}><Calculator/></div></div></div></div>) }