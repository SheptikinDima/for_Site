export function attachRailButtons(){const pairs=[['prev-1','next-1','rail-1'],['prev-2','next-2','rail-2']];pairs.forEach(([p,n,railId])=>{const rail=document.getElementById(railId);const prev=document.getElementById(p);const next=document.getElementById(n);if(rail&&prev&&next){const slide=rail.querySelector('.slide');const step=(slide&&slide.clientWidth)||320;prev.addEventListener('click',()=>rail.scrollBy({left:-step-16,behavior:'smooth'}));next.addEventListener('click',()=>rail.scrollBy({left:step+16,behavior:'smooth'}));rail.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)>Math.abs(e.deltaX)){e.preventDefault();rail.scrollLeft+=e.deltaY>0?step/2:-step/2}}, {passive:false});}})}